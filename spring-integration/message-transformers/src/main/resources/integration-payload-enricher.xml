<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd">

    <int:channel id="inputChannel5"/>
    <int:channel id="outputChannel5"/>
    <int:channel id="requestChannel5"/>
    <int:gateway service-interface="com.ddubson.gateways.PrinterGateway"
                 id="gateway5"
                 default-request-channel="inputChannel5"/>

    <int:enricher id="enricher5"
                  input-channel="inputChannel5"
                  request-channel="requestChannel5"
                  output-channel="outputChannel5">
        <!-- assign whole payload to the phonenumber field -->
        <int:property name="phoneNumber" expression="payload"/>
    </int:enricher>

    <int:service-activator id="customServiceRef" ref="customService" input-channel="requestChannel5" method="getPhoneNumber" />
    <bean id="customService" class="com.ddubson.services.CustomService" />

    <int:service-activator id="stringPrint5" ref="stringPrintService" method="print" input-channel="outputChannel5"/>
    <bean id="stringPrintService" class="com.ddubson.services.StringPrintService"/>
</beans>