<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:int="http://www.springframework.org/schema/integration"
       xmlns:task="http://www.springframework.org/schema/task"
       xsi:schemaLocation="
            http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task.xsd
            http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
            http://www.springframework.org/schema/integration http://www.springframework.org/schema/integration/spring-integration.xsd">
    <!-- task executor - thread pool of 5 -->
    <task:executor id="executor" pool-size="5"/>

    <int:publish-subscribe-channel id="interceptedChannel" task-executor="executor">
        <int:interceptors>
            <int:ref bean="customChannelInterceptor" />
        </int:interceptors>
    </int:publish-subscribe-channel>

    <int:gateway service-interface="com.ddubson.gateways.PrinterGateway"
                 id="interceptedChannelGateway" default-request-channel="interceptedChannel"/>

    <bean class="com.ddubson.CustomChannelInterceptor" id="customChannelInterceptor" />
    <bean class="com.ddubson.services.PrintService" id="printService"/>

    <int:service-activator ref="printService" method="print" input-channel="interceptedChannel"/>
</beans>